spring:
  application:
    name: split-payment-api

  datasource:
    url: jdbc:postgresql://localhost:5432/psp
    username: split
    password: split
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false